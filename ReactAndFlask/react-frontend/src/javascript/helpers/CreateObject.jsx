import axios from 'axios';

function getURL(endpoint) {
    return Constants.serverEndpoint + usersMainPath + endpoint;
}

export default function CreateObject(command, arguments) {
    return ;
}
