import {
	combineReducers
} from "redux";

const rootReducer = combineReducers({
	/**
	 * add all reducers here in form
	 * reducer1,
	 * reducer2,
	 * ...
	 */
})

export default rootReducer;
